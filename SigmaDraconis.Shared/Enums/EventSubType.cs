namespace SigmaDraconis.Shared
{
    public enum EventSubType
    {
        // World events
        Added, Updated, Removed, Completed, Ready, Recycling, PathUpdate, CommandsChanged, Moving,

        // Game events
        Loaded
    }
}
