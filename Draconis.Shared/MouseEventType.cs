namespace Draconis.Shared
{
    public enum MouseEventType {
        None, Move, LeftDrag, RightDrag, LeftDragRelease, RightDragRelease, LeftButtonUp, LeftButtonHold, RightButtonUp, LeftClick, RightClick, ScrollUp, ScrollDown, LeftDown, RightDown
    };
}
